#inclaude <iostream>
using namespace std;

struct node {
	int noMhs;
	string name;
	Node* next;
};

Node* START = NULL;

void addNode() {
	int nim;
	string nama;
	Node* nodeBaru = new Node();
	cout << "Masukkan NIM: ";
	cin >> nim;
	cout << "Masukkan Nama: ";
	cin >> nama;
	nodeBaru->noMhs = nim;
	nodeBaru->name = name;

	if (STAR == NULL || nim <= STAR->noMhs) {
		if (STAR != NULL && nim == STAR->noMhs)
		{

		}
	}
}